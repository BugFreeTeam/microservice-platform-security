<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mall.security.mapper.UmsRoleResourceRelationMapper">
    <resultMap id="ResultMap" type="com.mall.security.model.UmsRoleResourceRelation">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId" />  <!-- 角色ID bigint(20) -->
        <result column="resource_id" property="resourceId" />  <!-- 资源ID bigint(20) -->
    </resultMap>
    <!-- 自定义操作 -->


    <!-- 根据主键查询 -->
    <select id="selectById" resultMap="ResultMap" parameterType="java.lang.Long">
        select id, role_id, resource_id
        from ums_role_resource_relation where id = #{id}
    </select>
    <!-- 根据主键查询数据列表 -->
    <select id="selectByIds" resultMap="ResultMap" parameterType="java.util.List">
        select id, role_id, resource_id
        from ums_role_resource_relation where	id in
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
        order by id
    </select>
    <!-- 根据条件查询 -->
    <select id="selectList" resultMap="ResultMap" parameterType="com.mall.security.model.UmsRoleResourceRelation">
        select id, role_id, resource_id
        from ums_role_resource_relation
        <where>
            <if test="id != null">and id = #{id}</if>
            <if test="roleId != null">and role_id = #{roleId}</if>
            <if test="resourceId != null">and resource_id = #{resourceId}</if>
        </where>
        <choose>
            <when test="order != null">
                order by ${order}
            </when>
            <otherwise>
                order by id
            </otherwise>
        </choose>
    </select>
    <!-- 根据条件判断数据是否存在 -->
    <select id="exists" resultMap="ResultMap" parameterType="com.mall.security.model.UmsRoleResourceRelation">
        select id, role_id, resource_id
        from ums_role_resource_relation
        <where>
            <if test="id != null">and id = #{id}</if>
            <if test="roleId != null">and role_id = #{roleId}</if>
            <if test="resourceId != null">and resource_id = #{resourceId}</if>
        </where>
    </select>
    <!-- 新增数据 -->
    <insert id="save" parameterType="com.mall.security.model.UmsRoleResourceRelation">
        insert into ums_role_resource_relation
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="id != null">id, </if>
            <if test="roleId != null">role_id, </if>
            <if test="resourceId != null">resource_id, </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="id != null">#{id}, </if>
            <if test="roleId != null">#{roleId}, </if>
            <if test="resourceId != null">#{resourceId}, </if>
        </trim>
    </insert>
    <!-- 批量新增数据 -->
    <insert id="saveBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            insert into ums_role_resource_relation
            <trim prefix="(" suffix=")" suffixOverrides="," >
                <if test="item.id != null">id, </if>
                <if test="item.roleId != null">role_id, </if>
                <if test="item.resourceId != null">resource_id, </if>
            </trim>
            <trim prefix="values (" suffix=")" suffixOverrides="," >
                <if test="item.id != null">#{item.id}, </if>
                <if test="item.roleId != null">#{item.roleId}, </if>
                <if test="item.resourceId != null">#{item.resourceId}, </if>
            </trim>
        </foreach>
    </insert>
    <!-- 更新数据 -->
    <update id="update" parameterType="com.mall.security.model.UmsRoleResourceRelation">
        update ums_role_resource_relation
        <set>
            <if test="id != null">id = #{id}, </if>
            <if test="roleId != null">role_id = #{roleId}, </if>
            <if test="resourceId != null">resource_id = #{resourceId}, </if>
        </set>
        where id = #{id}
    </update>
    <!-- 批量更新数据 -->
    <update id="updateBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            update ums_role_resource_relation
            <set>
                <if test="item.id != null">id = #{item.id}, </if>
                <if test="item.roleId != null">role_id = #{item.roleId}, </if>
                <if test="item.resourceId != null">resource_id = #{item.resourceId}, </if>
            </set>
            where id = #{item.id}
        </foreach>
    </update>
    <!-- 逻辑删除数据 -->
    <update id="cancel" parameterType="com.mall.security.model.UmsRoleResourceRelation">
        update ums_role_resource_relation
        <set>
            flag = 0,
        </set>
        where id = #{id}
    </update>
    <!-- 批量逻辑删除数据 -->
    <update id="cancelBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            update ums_role_resource_relation
            <set>
                flag = 0,
            </set>
            where id = #{item.id}
        </foreach>
    </update>
    <!-- 物理删除数据 -->
    <delete id="delete" parameterType="java.lang.Long">
        delete from ums_role_resource_relation where id = #{id}
    </delete>
    <!-- 批量物理删除数据 -->
    <delete id="deleteBatch" parameterType="java.util.List">
        delete from ums_role_resource_relation where id in
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </delete>
</mapper>